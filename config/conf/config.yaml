defaults:
  - files: mnist
  - _self_
paths:
  log: ${hydra:runtime.cwd}/logs
  data: ${hydra:runtime.cwd}/data/raw
params:
  epoch_count: 20
  lr: 5e-5
  batch_size: 128
  shuffle: True
  num_workers: 2
